<nav class='navbar navbar-expand-lg bg-light sticky-top'>
  <div class='container'>

    {{!-- <div class="row">
      <div class="col-6"></div>
      <div class="col-6"></div>
    </div> --}}

    <a class='navbar-brand titulo d-flex align-items-center' href='#'>
        {{#if usuario.image}}
          <img
          src='/avatar/{{usuario.image}}'
          height='50px'
          class="avatar"
          />
        {{/if}}
        <p class="info pt-1 mb-0">
          {{usuario.nome}} <br>
          {{#usuario.Entidade}}
          <small class="text-muted">{{nome}}</small>
          {{/usuario.Entidade}}
       </p>
    </a>

    <div class='d-flex'>
      <div class='btn-group' role='group' aria-label='Basic example'>
        <button
          type='button'
          class='btn btn-primary'
          data-bs-toggle='modal'
          data-bs-target='#staticBackdrop'
        >
          Fazer Solicitação
        </button>
        <a href='/sair' class='btn btn-outline-danger' type='button'>
          Sair
        </a>
      </div>
    </div>
  </div>
</nav>

{{> modal-solicitacao}}
